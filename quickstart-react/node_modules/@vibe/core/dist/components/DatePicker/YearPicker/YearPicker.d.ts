import React from "react";
import { Moment } from "../types";
export interface YearPickerProps {
    /**
     * The currently selected date.
     */
    selectedDate?: Moment;
    /**
     * Function to determine if a year should be blocked.
     */
    isYearBlocked?: (year: number) => boolean;
    /**
     * Callback fired when changing the selected year.
     */
    changeCurrentDate: (date: Moment) => void;
    /**
     * Test ID for testing purposes.
     */
    "data-testid"?: string;
}
declare const YearPicker: ({ selectedDate, isYearBlocked, changeCurrentDate, "data-testid": dateTestId }: YearPickerProps) => React.JSX.Element;
export default YearPicker;
