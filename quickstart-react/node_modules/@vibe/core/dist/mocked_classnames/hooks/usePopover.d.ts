/// <reference types="react" />
import { Placement } from "./popoverConstants";
export default function usePopover(referenceElement: HTMLElement, popperElement: HTMLElement, { isOpen, placement, observeContentResize, offset, fallbackPlacements }: {
    isOpen?: boolean;
    placement?: Placement;
    observeContentResize?: boolean;
    offset?: [number, number];
    fallbackPlacements?: Placement[];
}): {
    styles: {
        [key: string]: import("react").CSSProperties;
    };
    attributes: {
        [key: string]: {
            [key: string]: string;
        };
    };
};
